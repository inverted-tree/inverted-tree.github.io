---
title: "License"
noHeader: true
noFooter: true
---

This page displays the license for your site.

To enable it:

1. Place your license text file in the `assets/` directory and name it exactly `license`.
2. Set the path to that file in your `hugo.toml` under `params` as `license`, pointing to the file name.
